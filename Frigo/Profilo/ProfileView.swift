import SwiftUI

struct ProfileView: View {
    var body: some View {
        Color.white
            .ignoresSafeArea()
            .navigationTitle("Profilo")
            .navigationBarTitleDisplayMode(.inline)
    }
}

#Preview {
    ProfileView()
}